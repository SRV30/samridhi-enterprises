import { useDispatch, useSelector } from "react-redux";
import { useEffect, useState } from "react";
import { clearAuthState, resetPassword } from "@/store/auth-slice/user";
import { useNavigate, Link } from "react-router-dom";
import MetaData from "../../extras/MetaData";
import { toast } from "react-toastify";
import { motion, AnimatePresence } from "framer-motion";
import { Mail, LockKeyhole, Eye, EyeOff, ArrowLeft, CheckCircle2 } from "lucide-react";
import { PASSWORD_REQUIREMENTS } from "../../utils/passwordRequirements";

const ResetPassword = () => {
  const [email, setEmail] = useState(""); const [otp, setOtp] = useState(""); const [newPassword, setNewPassword] = useState(""); const [confirmPassword, setConfirmPassword] = useState(""); const [showNew, setShowNew] = useState(false); const [showConfirm, setShowConfirm] = useState(false);
  const dispatch = useDispatch(); const navigate = useNavigate(); const { loading, error, success } = useSelector((state) => state.auth);
  useEffect(() => { const e = localStorage.getItem("resetEmail"); const o = localStorage.getItem("resetOtp"); if (e && o) { setEmail(e); setOtp(o); } else { localStorage.removeItem("resetEmail"); localStorage.removeItem("resetOtp"); toast.error("Please verify your OTP first."); navigate("/verify-otp"); } }, [navigate]);
  const submit = (e) => { e.preventDefault(); if (!email || !otp || !newPassword || !confirmPassword) return toast.error("All fields are required"); if (!PASSWORD_REQUIREMENTS.every((r) => r.test(newPassword))) return toast.error("Password does not meet complexity requirements!"); if (newPassword !== confirmPassword) return toast.error("Passwords do not match!"); dispatch(resetPassword({ email, otp, newPassword, confirmPassword })); };
  useEffect(() => { if (success) { toast.success("Password updated successfully!"); localStorage.removeItem("resetEmail"); localStorage.removeItem("resetOtp"); const timer = setTimeout(() => navigate("/login"), 1200); return () => clearTimeout(timer); } if (error) toast.error(error); }, [success, error, navigate]);
  useEffect(() => () => { dispatch(clearAuthState()); }, [dispatch]);
  return <><MetaData title="Reset Password | Samridhi Enterprises" description="Create a new secure password for your account." keywords="reset password, Samridhi Enterprises account security"/><main className="min-h-[calc(100vh-140px)] bg-[var(--surface-1)] px-4 py-10 sm:px-6"><motion.div initial={{ opacity: 0, y: 18 }} animate={{ opacity: 1, y: 0 }} className="mx-auto w-full max-w-xl rounded-[2rem] border border-[var(--line)] bg-[var(--surface-0)] p-6 shadow-[0_24px_80px_rgba(15,23,42,.1)] sm:p-10 lg:p-14"><div className="mx-auto max-w-md"><p className="text-xs font-bold uppercase tracking-[.18em] text-blue-600">Secure recovery</p><h1 className="mt-2 text-3xl font-black tracking-tight text-[var(--text-strong)]">Create a new password</h1><p className="mt-3 text-sm leading-6 text-[var(--text-muted)]">Choose a strong password to protect your Samridhi account.</p><form onSubmit={submit} className="mt-8 space-y-5"><label className="block"><span className="mb-2 block text-xs font-bold uppercase tracking-[.16em] text-[var(--text-muted)]">Account</span><span className="relative block"><Mail className="absolute left-4 top-1/2 -translate-y-1/2 h-4 w-4 text-blue-500"/><input id="reset-email" type="email" value={email} readOnly className="w-full cursor-not-allowed rounded-xl border border-[var(--line)] bg-[var(--surface-1)] py-3.5 pl-11 pr-4 text-sm text-[var(--text-muted)]"/></span></label><label className="block"><span className="mb-2 block text-xs font-bold uppercase tracking-[.16em] text-[var(--text-muted)]">New password</span><span className="relative block"><LockKeyhole className="absolute left-4 top-1/2 -translate-y-1/2 h-4 w-4 text-blue-500"/><input id="reset-new-password" type={showNew ? "text" : "password"} autoComplete="new-password" value={newPassword} onChange={(e) => setNewPassword(e.target.value)} placeholder="Create a strong password" className="w-full rounded-xl border border-[var(--line)] bg-[var(--surface-1)] py-3.5 pl-11 pr-12 text-sm text-[var(--text-strong)] outline-none focus:border-blue-500 focus:ring-4 focus:ring-blue-500/10"/><button type="button" onClick={() => setShowNew((v) => !v)} aria-label={showNew ? "Hide password" : "Show password"} className="absolute right-3 top-1/2 -translate-y-1/2 p-2 text-[var(--text-muted)]">{showNew ? <EyeOff className="h-4 w-4"/> : <Eye className="h-4 w-4"/>}</button></span></label><AnimatePresence>{newPassword && <motion.div initial={{ opacity: 0, height: 0 }} animate={{ opacity: 1, height: "auto" }} exit={{ opacity: 0, height: 0 }} className="overflow-hidden rounded-xl border border-[var(--line)] bg-[var(--surface-1)] p-4"><div className="grid gap-2 sm:grid-cols-2">{PASSWORD_REQUIREMENTS.map((r) => { const ok = r.test(newPassword); return <div key={r.id} className={`flex items-center gap-2 text-xs font-semibold ${ok ? "text-emerald-600" : "text-[var(--text-muted)]"}`}><CheckCircle2 className="h-4 w-4"/>{r.label}</div>; })}</div></motion.div>}</AnimatePresence><label className="block"><span className="mb-2 block text-xs font-bold uppercase tracking-[.16em] text-[var(--text-muted)]">Confirm password</span><span className="relative block"><LockKeyhole className="absolute left-4 top-1/2 -translate-y-1/2 h-4 w-4 text-blue-500"/><input id="reset-confirm-password" type={showConfirm ? "text" : "password"} autoComplete="new-password" value={confirmPassword} onChange={(e) => setConfirmPassword(e.target.value)} placeholder="Repeat your password" className="w-full rounded-xl border border-[var(--line)] bg-[var(--surface-1)] py-3.5 pl-11 pr-12 text-sm text-[var(--text-strong)] outline-none focus:border-blue-500 focus:ring-4 focus:ring-blue-500/10"/><button type="button" onClick={() => setShowConfirm((v) => !v)} aria-label={showConfirm ? "Hide password" : "Show password"} className="absolute right-3 top-1/2 -translate-y-1/2 p-2 text-[var(--text-muted)]">{showConfirm ? <EyeOff className="h-4 w-4"/> : <Eye className="h-4 w-4"/>}</button></span></label><button disabled={loading} className="w-full rounded-xl bg-blue-600 px-5 py-3.5 text-sm font-bold text-white shadow-lg shadow-blue-600/20 hover:bg-blue-700 disabled:opacity-60">{loading ? "Updating password…" : "Update password"}</button></form><div className="mt-7 flex justify-center"><Link to="/login" className="inline-flex items-center gap-2 text-sm font-bold text-[var(--text-muted)] hover:text-blue-600"><ArrowLeft className="h-4 w-4"/>Back to sign in</Link></div></div></motion.div></main></>;
};
export default ResetPassword;
# Dependency vulnerability scanning for both npm workspaces.
#  - dependency-review: runs npm audit against a generated lockfile so
#    repositories without committed lockfiles can still be audited.
#  - npm-audit: full advisory report for each workspace, reported (not gated)
#    so existing advisories are visible without blocking every PR. Runs weekly.
name: "Dependency Audit"

on:
  pull_request:
    branches: [main, elusoc]
  schedule:
    - cron: "30 2 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  dependency-review:
    name: Dependency Review (PR)
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        workspace: [client, server]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "20"
      - name: Generate lockfile
        working-directory: ${{ matrix.workspace }}
        run: npm install --package-lock-only --ignore-scripts
      - name: Run npm audit (${{ matrix.workspace }})
        working-directory: ${{ matrix.workspace }}
        run: npm audit --audit-level=high

  npm-audit:
    name: npm audit (${{ matrix.workspace }})
    runs-on: ubuntu-latest
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    strategy:
      fail-fast: false
      matrix:
        workspace: [client, server]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "20"
      - name: Generate lockfile
        working-directory: ${{ matrix.workspace }}
        run: npm install --package-lock-only --ignore-scripts
      - name: Run npm audit (${{ matrix.workspace }})
        working-directory: ${{ matrix.workspace }}
        continue-on-error: true
        run: npm audit --audit-level=high
